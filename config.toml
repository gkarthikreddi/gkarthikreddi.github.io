# The URL the site will be built for
base_url = "https://karthikreddy.org/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# The site title and description; used in feeds by default.
title = "Karthik Reddy"
description = ""

# The default language; used in feeds.
default_language = "en"

# The site theme to use
theme = "apollo"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "dracula"

[extra]
# Put all your custom variables here
favicon = "/favicon.png"
menu = [
    { name = "/posts", url = "/posts", weight = 1 },
    { name = "/projects", url = "/projects", weight = 2 },
    { name = "/about", url = "/about", weight = 3 },
]

socials = [
    { name = "github", url = "https://github.com/gkarthikreddi/", icon = "github" },
    { name = "linkedin", url = "https://linkedin.com/in/gkarthikreddi/", icon = "linkedin" },
    { name = "email", url = "mailto:me@karthikreddy.org", icon = "email" },
]
